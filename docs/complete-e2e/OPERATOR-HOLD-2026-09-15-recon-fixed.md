# power-claude complete-e2e hold notes (2026-09-15 night)

## Fixed this fire
- reconciliation_gaps / runtime_only: dual-origin `scripts/bin/*.sh` + list-surfaces path align → gap_count=0
- cli valid `cli_exit_2` / `cli_missing_help_text`: wrappers map engine project-dir valid grain → `--help`

## Still holding
- actor:guest `actor.session-established` → `receipt_identity_mismatch` after run resets (t0/named receipt identity drift across resume-created runs). Not inventing CERTIFIED.
- Certificate may still DENIED for reviewers/proof_kernel/clean_replay/outstanding until actor cell greens stably.
